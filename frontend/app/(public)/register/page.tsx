// ─────────────────────────────────────────────────────────────
// PLACEHOLDER — Frontend will implement this page
// ─────────────────────────────────────────────────────────────
// LAYOUT: split panel — left: feature list · right: form
// FEATURES: name + email + password · link to /login
// AUTH: use supabase.auth.signUp() then create workspace row
// ─────────────────────────────────────────────────────────────
export default function RegisterPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-bg">
      <div className="text-center flex flex-col gap-2">
        <h1 className="font-display text-xl font-semibold text-foreground">Create workspace</h1>
        <p className="text-subtle text-xs">[ Frontend placeholder — replace with full implementation ]</p>
      </div>
    </div>
  )
}
